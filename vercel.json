{
  "buildCommand": "cd cleary && npm install && npm run build",
  "outputDirectory": "cleary/build",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
