{
  "name": "cleary-root",
  "private": true,
  "scripts": {
    "build": "cd cleary && npm install && npm run build",
    "dev": "cd cleary && npm start",
    "vercel-build": "cd cleary && npm install --no-audit --no-fund && npm run build"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "og": "^0.0.2",
    "ogl": "^1.0.11",
    "pg": "^8.16.3",
    "googleapis": "^131.0.0",
    "jsonwebtoken": "^9.0.2"
  }
}
